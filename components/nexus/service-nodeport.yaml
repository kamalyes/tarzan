apiVersion: v1
kind: Service
metadata:
  name: nexus
  namespace: {{COMPONENT_NAMESPACE}}
  labels:
    app.kubernetes.io/name: nexus
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: nexus
  ports:
    - name: web
      port: 8081
      targetPort: web
      nodePort: 30023
