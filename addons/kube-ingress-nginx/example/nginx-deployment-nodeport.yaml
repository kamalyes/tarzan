apiVersion: v1
kind: Namespace
metadata:
  name: kube-example  # 创建命名空间 kube-example
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: kube-example
  name: ndp-nginx
  labels:
    app: ndp-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ndp-nginx
  template:
    metadata:
      labels:
        app: ndp-nginx
    spec:
      containers:
      - name: ndp-nginx
        image: {{ADDONS_IMAGE_REPOSITORY}}/nginx:1.25.2-alpine3.18-slim
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "250m"
            memory: "600Mi"
          limits: 
            cpu: "500m"
            memory: "1024Mi" 
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  namespace: kube-example
  name: ndp-nginx-svc
spec:
  selector:
    app: ndp-nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      # NodePort 会自动分配一个端口，您也可以指定一个范围
      nodePort: 30001
  type: NodePort
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: ndp-nginx-hpa-c
  namespace: kube-example
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ndp-nginx
  minReplicas: 1
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: cpu
      targetAverageUtilization: 80
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: ndp-nginx-hpa-m
  namespace: kube-example
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ndp-nginx
  minReplicas: 1
  maxReplicas: 2
  metrics:
  - type: Resource
    resource:
      name: memory
      targetAverageUtilization: 80